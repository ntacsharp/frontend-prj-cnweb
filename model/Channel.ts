export class Channel {
    constructor(private id: string, private name: string, private type: string, private serverId: string, private createdAt: Date, private updatedAt: Date,) {

        id = id;
        name = name;
        type = type;
        serverId = serverId;
        createdAt = createdAt;
        updatedAt = updatedAt;

    }
}